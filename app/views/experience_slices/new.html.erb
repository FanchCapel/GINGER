<div class="container">
<h2>Créer l'Experience</h2>

  <p>Monsieur et Madame <%= @experience.user.primary_last_name.capitalize %> ont un budget de <%= humanized_money @experience.budget_cents %> CHF. Ils souhaitent sortir sur <%= @experience.city %>, le <%= @experience.date.to_date %> <%= @experience.time_slot %></p>

  <div class="d-flex justify-content-between">
    <p>Budget: <%= humanized_money_with_symbol @experience.budget_cents %> </p>
    <p>Prix total: <span id="prix">0</span> CHF</p>
    <p>Durée totale: <span id="duration">0</span></p>
  </div>
  <%= render 'shared/exp_design_form' %>

  <!-- Tab links -->
<div class="selections d-flex justify-content-between mt-3">
    <div class="tab-wrapper">
      <div class="tab">
        <% @categories.each do |category| %>
          <button class="tablinks <%= 'active' if category.name == 'Food' %>" id="<%= category.name %>"><%= category.name %></button>
        <% end %>
      </div>
      <div id="content-list" style="color:black">
        <% @activities.each do |activity| %>
          <div class="draggable tabcontent list-group-item content-wrapper <%= activity.category.name %> <%= 'default' if activity.category.name == 'Food' %>" draggable="true"><%= "#{activity.name} - #{activity.price_cents / 100} CHF - #{activity.duration} mins" %></div>
        <% end %>
      </div>
    </div>

    <div id="rank-list" style="color:black">
      <div id = "pipeline-rank-row-1">
        <div class="pipeline-rank-number">1</div>
        <div id = "pipeline-rank-1" class="pipeline-holder holder-empty dropzone" dropzone="move"></div>
      </div>
      <div id = "pipeline-rank-row-2">
        <div class="pipeline-rank-number">2</div>
        <div id = "pipeline-rank-2" class="pipeline-holder holder-empty dropzone" dropzone="move"></div>
        <div class ="remember-my-position-dropped"></div>
        <div class ="remember-my-position-hover"></div>
      </div>
      <div id = "pipeline-rank-row-3">
        <div class="pipeline-rank-number">3</div>
        <div id = "pipeline-rank-3" class="pipeline-holder holder-empty dropzone" dropzone="move"></div>
        <div class ="remember-my-position-dropped"></div>
        <div class ="remember-my-position-hover"></div>
      </div>
    </div>
  </div>
</div>



