<div class="container">
  <h2>Créer l'Experience</h2>

  <p>Monsieur et Madame <%= @experience.user.primary_last_name.capitalize %> ont un budget de <%= humanized_money @experience.budget_cents %> CHF. Ils souhaitent sortir sur <%= @experience.city %>, le <%= @experience.date.to_date %> <%= @experience.time_slot %></p>

  <div class="d-flex justify-content-between">
    <p>Budget: <%= humanized_money_with_symbol @experience.budget_cents %> </p>
    <p>Prix total: <span id="prix">0</span> CHF</p>
    <p>Durée totale: <span id="duration">0</span></p>
  </div>
  <%= render 'shared/exp_design_form_2' %>

  <!-- Tab links -->
  <div class="selections d-flex justify-content-between mt-3">
    <div class="tab-wrapper">
      <div class="tab">
        <% @categories.each do |category| %>
          <button class="tablinks <%= 'active' if category.name == 'Food' %>" id="<%= category.name %>"><%= category.name %></button>
        <% end %>
      </div>
      <div class="sortable-activities" dropzone="move" id="content-list" style="color:black">
        <% @activities.each do |activity| %>
          <div class="draggable tabcontent list-group-item content-wrapper <%= activity.category.name %> <%= 'default' if activity.category.name == 'Food' %>" draggable="true" name='<%= "activity-#{activity.id}" %>' value=<%= activity.id %>><%= "#{activity.name} - #{activity.price_cents / 100} CHF - #{activity.duration} mins" %></div>
        <% end %>
      </div>
    </div>
  </div>
</div>



