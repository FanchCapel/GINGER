<div class="container">
  <div class="row py-4">
    <div class="col">
      <h1>Resumé de votre experience</h1>
      <p>Organisation de <strong><%= "Mon Date" %></strong> pour un budget total de <strong><%= humanized_money_with_symbol @experience.budget_cents %></strong></p>
      <button id="pay" class="btn btn-primary">Payer</button>
      <script src="https://js.stripe.com/v3/"></script>
      <script>
        const paymentButton = document.getElementById('pay');
        paymentButton.addEventListener('click', () => {
          const stripe = Stripe('<%= ENV['STRIPE_PUBLISHABLE_KEY'] %>');
          stripe.redirectToCheckout({
            sessionId: '<%= @experience.checkout_session_id %>'
          });
        });
      </script>
    </div>
  </div>
</div>
